# 셋이 똑같은 버전을 쓰기 위한 고정 목록 (pip freeze 기준, 2026-08-24).
# 설치:  python3 -m venv venv && source venv/bin/activate
#        pip install -r requirements.txt
# 라이브러리를 새로 추가했다면 pip freeze > requirements.txt 로 갱신하고 팀에 알린다.

# --- 직접 쓰는 것 ---
Flask==3.1.3
pymongo==4.17.0
PyJWT==2.13.0
python-dotenv==1.2.3
Flask-SocketIO==5.6.1

# --- 위 넷이 끌고 오는 의존성 (직접 import 하지 않는다) ---
blinker==1.9.0
click==8.4.2
dnspython==2.8.0
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3
Werkzeug==3.1.8
bidict==0.24.0
h11==0.16.0
python-engineio==4.13.5
python-socketio==5.16.4
simple-websocket==1.1.0
wsproto==1.3.2
